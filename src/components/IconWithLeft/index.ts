import styled from "styled-components";

const IconWithLeft = styled.span`
    position: absolute;
    right: 12px;
    color: #0295FE;
    font-size: 24px;
    top: 50%;
    transform: translateY(-50%);
    height: 100%;
    display: flex;
    align-items: center;
`
export default IconWithLeft
