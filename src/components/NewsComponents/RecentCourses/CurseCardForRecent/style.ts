import styled from "styled-components";

interface ProgressI {
  progress:
}

const CourseTitle = styled.p`
  font-size: 1.2em;
`

const MetaContainer = styled.div`
  
  
`

const CourseProgressBar = styled.div`
  width: ;
`

export {CourseTitle, MetaContainer}
